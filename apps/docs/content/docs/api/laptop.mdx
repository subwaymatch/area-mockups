---
title: Laptop (MacBook Air + MacBook Pro)
description: MacBook Air 13" and MacBook Pro 14" (M5) style laptops with a real keyboard layout and a live desktop display.
---

`<Laptop>` and `<LaptopMockup>` render an Apple-style laptop: unibody deck
with the real 78-key Magic Keyboard layout (printed legends, rendered as a
single instanced draw call), trackpad, hinge band, ports, and a notched
display. Two variants at true relative size: the MacBook Air 13" (M5) and the
MacBook Pro 14" (M5) — the Pro adds its scan-accurate port array (MagSafe,
2× Thunderbolt and headphone jack left; HDMI, Thunderbolt and SDXC right),
perforated speaker strips flanking the keyboard, bigger feet, the lid badge
and the embossed underside wordmark.

The window on screen is real DOM — click it, drag the mockup.

<ObjectDemo demo="laptop-basic" checker />

```tsx
<LaptopMockup color="#aec6d9" deviceProps={{ rotation: [0, -0.35, 0] }}>
  <YourDesktopApp />
</LaptopMockup>
```

## Props

Beyond the [shared screen props](/docs/screen-content):

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `variant` | `'air13' \| 'air15' \| 'pro14' \| 'pro16'` | `'air13'` | MacBook Air 13"/15" or MacBook Pro 14"/16" — true relative sizes, per-model ports, feet, speaker grilles and notch. |
| `colorway` | `string` | — | A retail colorway id from `LAPTOP_COLORWAYS` (Airs: `silver`, `starlight`, `midnight`, `skyblue`; Pros: `spaceblack`, `silver`). Presets `color`; an explicit `color` prop overrides it. |
| `color` | `string` | `'#e3e4e6'` | Aluminum finish. Try Sky Blue `#aec6d9`, Starlight `#e8e0d4`, Midnight `#2e3642`, or Space Black `#4a484b` on the Pros. |
| `notch` | `boolean` | `true` | Camera notch overlay at the top of the display. |
| `openAngle` | `number` | `110` | Lid angle in degrees between deck and screen (90 is upright). |
| `resolution` | `number` | per variant | Virtual display width — each variant defaults to its real scaled desktop: 1280×832 (air13), 1440×932 (air15), 1512×982 (pro14), 1728×1117 (pro16) — so desktop breakpoints apply. |

There is no `orientation` prop; laptops only have one pose.

## More examples

### MacBook Pro 14" in Space Black

`variant="pro14"`: deeper body, HDMI + SDXC on the right flank, speaker
grilles beside the keyboard, and the 1512×982 point grid.

<ObjectDemo demo="laptop-pro14" />

```tsx
<LaptopMockup variant="pro14" color="#4a484b">
  <YourDesktopApp />
</LaptopMockup>
```

### MacBook Air 15" in Sky Blue

`variant="air15"`: the 15.3" Air — same thin slab and clean deck as the
13", scaled to its true 340×238 mm footprint and 1440×932 point grid.

<ObjectDemo demo="laptop-air15" />

```tsx
<LaptopMockup variant="air15" colorway="skyblue">
  <YourDesktopApp />
</LaptopMockup>
```

### MacBook Pro 16" in Space Black

`variant="pro16"`: the biggest MacBook — wider speaker grilles, larger
trackpad and the 1728×1117 point grid.

<ObjectDemo demo="laptop-pro16" />

```tsx
<LaptopMockup variant="pro16" colorway="spaceblack">
  <YourDesktopApp />
</LaptopMockup>
```

### Midnight, tighter lid angle

`openAngle` poses the hinge; 100° reads more "working", 110° more "display".

<ObjectDemo demo="laptop-midnight" />

```tsx
<LaptopMockup color="#2e3642" openAngle={100} float>
  <YourDesktopApp />
</LaptopMockup>
```
