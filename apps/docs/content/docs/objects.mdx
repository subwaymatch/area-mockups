---
title: Objects and print surfaces
description: Books, magazines, brochures, business cards, poster frames, billboards, and a wrap-ready cargo van — every non-device mockup in the library.
---

Beyond phones and laptops, the library models everyday objects whose printable
surface is a live DOM area: design a book cover or a van livery in plain React
(or drop in an `<iframe>`), and it stays interactive on the 3D object.

Like the devices, every object is procedurally generated at runtime from real
product dimensions — no GLB files, no hosting, no pop-in.

## The objects

| Object | Component | Real size | Live area | Default virtual px |
| --- | --- | --- | --- | --- |
| Hardcover book | `Book` | 156×234×30 mm trade hardcover | front cover | 480×720 |
| Magazine | `Magazine` | 216×279×6 mm perfect-bound | front cover | 480×620 |
| Tri-fold brochure | `Brochure` | 93×216 mm per letter-fold panel | 3 panels, front + back | 360×835 each |
| Business card | `BusinessCard` | 89×51 mm, 32 pt stock | front + back | 520×298 each |
| ID card (lanyard) | `IDCard` | 54×85.6 mm CR80 badge | front + back below the punch strip | 420×556 each |
| Poster frame | `PosterFrame` | 18"×24" sheet, 22 mm molding | visible opening (optional mat) | 540×721 |
| Product box | `ProductBox` | 190×265×55 mm retail carton | front + side + top + back | 480×669 front |
| Roll-up banner | `RollupBanner` | 850×2000 mm stand | banner graphic | 420×988 |
| Billboard | `Billboard` | 14'×48' highway bulletin | vinyl face | 1200×350 |
| City bus | `Bus` | 12.2×3.2×2.6 m transit bus | king-size ads both sides + tail ad + LED sign | 960×200 ad |
| Cargo van | `Van` | 5.9×2.55×2.05 m panel van | wrap panels both sides + rear doors | 900×368 |
| Bus shelter | `BusShelter` | ~4.3×2.5×1.45 m shelter | backlit 6-sheet, both faces | 480×709 each |
| Greeting card | `GreetingCard` | 127×178 mm folded A7 | covers + inside spread (4 faces) | 380×533 each |
| Vinyl record | `VinylRecord` | 313 mm jacket + 302 mm LP | cover + back + circular label | 520×520 cover |
| TV | `TVSet` | 65" 16:9 on blade feet | screen | 1920×1080 |
| A-frame sign | `AFrameSign` | 600×900 mm sandwich board | both panels | 420×635 each |
| DOOH totem | `DOOHTotem` | ~1.3×2.8×0.35 m street totem | portrait display | 540×960 |
| Storefront | `Storefront` | 6×4.2 m shop façade | fascia sign + window poster | 800×90 sign |
| Semi trailer | `SemiTrailer` | 16.2×2.6×2.9 m 53 ft dry van | wraps both sides + rear doors | 1100×189 side |
| Wheatpaste wall | `WheatpasteWall` | ~4.5×3 m brick wall | up to 3 one-sheet posters | 440×623 each |
| Mailer box | `MailerBox` | 350×120×250 mm shipper | top + front + end panels | 520×371 top |
| Shopping bag | `ShoppingBag` | 320×420×140 mm carrier | front + back below the cuff | 460×526 each |
| Coffee cup | `CoffeeCup` | 12/16 oz hot cup + sleeve | **curved** sleeve wrap (16 strips) | 1024×223 unrolled |
| Pizza box | `PizzaBox` | 356×356×45 mm 14" box | lid top + front flap + inside lid | 560×560 top |

Each object also has a one-liner `*Mockup` wrapper (`BookMockup`,
`VanMockup`, …) that bundles the stage, camera, and grounded shadow — the same
pattern as the devices.

```tsx
'use client'

import { BookMockup } from 'area-mockups'

export function CoverPreview() {
  return (
    <BookMockup color="#1f3a5f" float>
      <YourCoverArt /> {/* any React node, full bleed */}
    </BookMockup>
  )
}
```

Every object's API page has multiple live, draggable examples with the code
next to them — see the API reference section in the sidebar.

## Multiple live surfaces

Several objects host more than one surface:

- **`Brochure`** renders three panels in a standing zig-zag. Pass one node as
  `children` for the first panel, or all three via
  `panels={[<Front />, <Middle />, <Back />]}` — and print the reverse side
  with `backPanels`. `foldAngle={0}` lays the sheet flat.
- **`BusinessCard`** and **`IDCard`** take `children` for the front and
  `back` for the reverse — spin them around to see it.
- **`ProductBox`** prints up to four panels: `children` (front), `side`,
  `top` and `back`, all sharing one dpi.
- **`Bus`** pairs the king-size ad panel with an optional live LED
  `destinationSign` above the windshield.
- **`SemiTrailer`** wraps both 53 ft sides plus the rear doors — the door
  panel sits under a lock-rod overlay like a real wrap.
- **`WheatpasteWall`** pastes up to three overlapping posters via
  `posters={[...]}`.
- **`MailerBox`** prints the top, front and end panels, with the packing
  tape overlaid on your print.
- **`Storefront`** pairs the fascia sign (`children`) with a `windowPoster`
  in the display glazing.
- **`PizzaBox`** prints the lid top, the front flap, and — when `open` —
  the inside of the lid.

## Curved surfaces

**`CoffeeCup`** is the first curved live surface: you pass ONE artwork at
the full unrolled wrap size, and the cup tiles it around the kraft sleeve
from 16 flat strips, each clipping its slice of the same DOM. Strips hide
themselves as they turn away, so the wrap reads continuous while spinning.
The trade-off: the artwork is duplicated per strip, so interactive/stateful
content won't stay in sync across the curve — curved wraps want art. The
same technique will carry future curved objects (bottles, cans, mugs).

## Ground planes

`VanMockup` and `BillboardMockup` ground their shadow at the wheels and pole
base respectively, like `MonitorMockup` does with its stand. The floating
`float` idle animation is available on every object, but reads most natural on
the hand-held print pieces.

## Renderer-agnostic specs

As with the devices, the raw dimensions are exported as plain data: `BOOK`,
`MAGAZINE`, `BROCHURE`, `BUSINESS_CARD`, `ID_CARD`, `POSTER_FRAME`,
`BILLBOARD`, and `VAN`, plus aspect-ratio constants like `VAN_WRAP_ASPECT`.
The same numbers will drive the planned 2D (CSS/SVG) renderers.
